---
import { Image } from "astro:assets";
import { getEntry, render } from "astro:content";
import { marked } from "marked";
import build from "../../assets/build.svg";
import Input from "../../components/input.astro";
import Select from "../../components/select.astro";
import Layout from "../../layouts/default.astro";

const page = await getEntry("pages", "donate");
const content = marked.parse(page.data?.content);
---

<Layout>
  <main class="h-full p-8 md:p-16 pb-0! flex flex-col gap-32">
    <section class="size-full flex flex-wrap justify-center items-center gap-8">
      <div class="w-md [&_a]:link">
        <h1>{page.data.heading}</h1>
        <Fragment set:html={content} />
      </div>
      <Image src={build} alt={page.data.photoAltText} class="w-full max-w-md" />
    </section>
    <section
      class="w-[calc(100% + 2rem)] bg-secondary px-8 md:px-16 py-32 -mx-8 md:-mx-16 text-white"
    >
      <h1 class="text-center mb-16!">Pledge</h1>
      <div class="flex flex-wrap items-start justify-center gap-16">
        <form id="form" class="w-xs flex flex-col items-end text-black!">
          <input
            type="hidden"
            name="access_key"
            value="02f9fed2-c3c5-44ba-a07b-970ddeab6b80"
          />
          <Input name="name" label="Name" placeholder="Jane Doe" required />
          <Input
            type="email"
            name="email"
            label="Email"
            placeholder="jane.doe@gmail.com"
            required
          />
          <Select
            name="pledge"
            label="Pledge Amount"
            placeholder="I am willing to give..."
            options={[
              "$5",
              "$10",
              "$25",
              "$50",
              "$100",
              "$250",
              "$415 (Max allowed)",
              "Other",
            ]}
            required
          />
          <button id="submit-button" type="submit" class="btn btn-accent mt-4"
            >Send</button
          >
        </form>
        <div class="min-w-3xs w-lg max-w-full">
          <h2>Commit to a better Denver.</h2>
          <p class="font-light">
            Donations will open up on August 5th. Let us know your committment,
            and we'll remind you when the time comes!
          </p>
        </div>
      </div>
    </section>
  </main>
</Layout>

<script>
  const form = document.getElementById("form") as HTMLFormElement;
  const submitButton = document.getElementById("submit-button") as HTMLElement;

  form.addEventListener("submit", async (e) => {
    e.preventDefault();
    submitButton.innerHTML = "Sending...";

    const response = await fetch("https://api.web3forms.com/submit", {
      method: "POST",
      body: new FormData(form),
    });

    const data = await response.json();
    if (data.success) {
      window.location.href = "/thank-you";
    } else {
      result.innerHTML = data.message;
    }
    window.posthog?.capture("contact_form_submitted");
  });
</script>
