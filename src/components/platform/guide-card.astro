---
import { Image } from 'astro:assets'
import { Marked } from 'marked'
import type { Lang } from '../../lib/platform'
import { localized } from '../../lib/platform'
import type { GuideEntry, PlankEntry } from '../../lib/platform-content'
import Icon from '../icon.astro'

interface Props {
  guide: GuideEntry
  planks: PlankEntry[]
  lang: Lang
}

const { guide, planks, lang } = Astro.props

const title = localized(guide.data, 'title', lang) ?? guide.data.title
const stance = localized(guide.data, 'stance', lang) ?? guide.data.stance
// The card's whole content sits inside one link (see the <a> below), so a
// markdown link in `stance` would render as an invalid nested <a> and break
// click-through for that card. Render links as plain text instead.
const stanceHtml = new Marked({
  renderer: { link: ({ text }) => text },
}).parse(stance)
const artwork = guide.data.artwork
// Blank means decorative, as in guide-hero.astro, which explains why.
const artworkAlt = localized(guide.data, 'artworkAlt', lang) ?? ''
const href =
  lang === 'es'
    ? `/es/platform/${guide.data.slug}`
    : `/platform/${guide.data.slug}`
---

<article>
  <a
    href={href}
    aria-label={title}
    class="block h-full border border-black/15 rounded-2xl p-6 text-black bg-base no-underline transition-transform duration-200 ease-out hover:-translate-y-1 active:-translate-y-1 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary"
  >
    {
      /* The issue's illustration at full card width, cropped to a square so
         a tall/portrait piece of art doesn't blow out the card's height
         (Q6). The small icon only stands in for a guide that has no artwork
         yet. */
      artwork && (
        <Image
          src={artwork}
          alt={artworkAlt}
          class="w-full aspect-square object-cover rounded-xl mb-5"
        />
      )
    }
    <h2 class="inline-flex items-center gap-2 text-2xl">
      {!artwork && <Icon name={guide.data.lucideIcon} class="size-7" />}
      {title}
    </h2>
    <div
      class="mt-1 font-light text-black/70 [&_strong]:font-bold [&_p]:text-base"
      set:html={stanceHtml}
    />
    {
      planks.length > 0 && (
        <ul class="list-none pl-0 mt-4 border-t border-black/10">
          {planks.map((plank, index) => (
            <li class="flex gap-3 items-baseline py-2.5 border-b border-black/5 last:border-b-0">
              <b class="text-primary text-xs tracking-wider shrink-0">
                {String(index + 1).padStart(2, '0')}
              </b>
              <span class="text-base leading-snug">
                {localized(plank.data, 'commitment', lang) ??
                  plank.data.commitment}
              </span>
            </li>
          ))}
        </ul>
      )
    }
  </a>
</article>
