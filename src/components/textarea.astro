---
import Fieldset from "./fieldset.astro";

const { name, label, placeholder, required } = Astro.props;
---

<Fieldset label={label}
  ><textarea
    name={name}
    class="textarea w-full text-base"
    placeholder={placeholder}
    required={required}></textarea></Fieldset
>
